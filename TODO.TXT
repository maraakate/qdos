* Fix ref_gl to be like Q2 so it's dynamic.

* Fix ref_soft to have Q2's version that is not tied to that crappy
  SciTech lib.

* Would be nice to have x64 working so there could be raw input mouse
  version for Windows.

* Unify and update the docs.

* Asynchronous rendering support.

* ref_gl mtex is incredibly slow with mesa and sage.  Completely unusable.
  Figure out why.

* When changing 'game' dynamically we should reload the WADs for
  console backdrop, etc.

* Fix Win32 requiring to set -ip if you have multiple adapters.

* Skin downloading is broken if it doesn't exist on the HTTP server.
